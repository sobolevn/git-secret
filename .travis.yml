language: c

sudo: false

env:
  - SECRETS_GPG_COMMAND=gpg
  - SECRETS_GPG_COMMAND=gpg2
  
branches:
  only:
    - master
    - develop

install:
  - make install-test

script:
  - make test

notifications:
  email:
    on_success: never
    on_failure: change
